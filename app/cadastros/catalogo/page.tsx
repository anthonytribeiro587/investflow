import { Shell } from "@/components/Shell";
import { catalog } from "@/lib/data";
export default function Catalogo(){return <Shell title="Catálogo de investimentos" subtitle="Setores, itens e projetos para padronizar a solicitação e evitar digitação livre descontrolada."><div className="two-col"><section className="panel"><h2>Setores</h2>{catalog.setores.map(x=><div className="list-item" key={x}><span>{x}</span><strong>Ativo</strong></div>)}</section><section className="panel"><h2>Projetos padrão</h2>{catalog.projetos.map(x=><div className="list-item" key={x}><span>{x}</span><strong>Ativo</strong></div>)}</section></div><section className="panel spaced"><h2>Itens de catálogo</h2><div className="chip-row">{catalog.itens.map(x=><span className="chip" key={x}>{x}</span>)}</div></section></Shell>}
